<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Blog - <%= blog.title %></title>
  <link rel="stylesheet" href="/css/styles.css">

</head>
<body>
  <h1>Edit Blog: <%= blog.title %></h1>

  <!-- Form to edit the blog -->
  <form action="/blogs/<%= blog.id %>/edit" method="POST">
    <div>
      <label for="title">Title</label>
      <input type="text" id="title" name="title" value="<%= blog.title %>" required>
    </div>
    
    <div>
      <label for="content">Content</label>
      <textarea id="content" name="content" required><%= blog.content %></textarea>
    </div>
    <% if (blog.image_path) { %>
      <img src="/uploads/<%= blog.image_path %>" alt="Current Image" width="200" />
    <% } %>
  
    <button type="submit">Update Blog</button>
  </form>

  <br>
  <a href="/blogs/<%= blog.id %>">Cancel</a> <!-- Link to view the blog without saving changes -->

</body>
</html>
